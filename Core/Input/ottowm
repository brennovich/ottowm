# OttoWM example configuration
#
# One `key combo = action` or `setting = value` per line.

# Display the pager at the right bottom corner, at the moment it contains
# a badge component that shows the current workspace.
pager = on

# Points kept between a maximized window and the screen edges and between two
# tiled windows, and the amount move-window and resize change a window by.
# 15 when left out.
# spacing = 15

# Go to a workspace: 4 Workspaces example
lopt-1 = switch-to-workspace 1
lopt-2 = switch-to-workspace 2
lopt-3 = switch-to-workspace 3
lopt-4 = switch-to-workspace 4

# Send the current focused window to a workspace: 4 workspaces example
lopt-shift-1 = move-window-to-workspace 1
lopt-shift-2 = move-window-to-workspace 2
lopt-shift-3 = move-window-to-workspace 3
lopt-shift-4 = move-window-to-workspace 4

# Move the focus to the window a direction leads to, within the current workspace
lopt-h = focus west
lopt-j = focus south
lopt-k = focus north
lopt-l = focus east

# Move the focused window a direction, by the spacing
lopt-shift-h = move-window west
lopt-shift-j = move-window south
lopt-shift-k = move-window north
lopt-shift-l = move-window east

# Resize the focused window from its top left corner, by the spacing
lopt-ctrl-shift-h = resize narrower
lopt-ctrl-shift-j = resize taller
lopt-ctrl-shift-k = resize shorter
lopt-ctrl-shift-l = resize wider

# Center the focused window on the screen, keeping its size
lopt-ctrl-c = center-window

# Fill the screen with the focused window, and put it back where it was on the next press
lopt-ctrl-m = maximize

# Tile the focused window to half the screen, and put it back on the next press
lopt-ctrl-h = tile west
lopt-ctrl-j = tile south
lopt-ctrl-k = tile north
lopt-ctrl-l = tile east

# Quit OttoWM, putting every parked window back where it was
hyper-q = quit

# Reload the config file and the bindings it names
hyper-r = restart
